import { SearchFaqPipe } from './search-faq.pipe';

describe('SearchFaqPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFaqPipe();
    expect(pipe).toBeTruthy();
  });
});
