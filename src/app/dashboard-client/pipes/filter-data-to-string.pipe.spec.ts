import { FilterDataToStringPipe } from './filter-data-to-string.pipe';

describe('FilterDateToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDataToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
